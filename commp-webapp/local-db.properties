## Properties used by gradle build for tasks for working with database on localhost
## Specify the values below by replacing placeholder in angles

#jdbc.url=jdbc:mysql://localhost:3306/my_test_comms_db

#After changing the properties use following command to tell git to ignore the local changes.
#git update-index --assume-unchanged <file-name>

#file-name=commp-webapp/local-db.properties

jdbc.url=jdbc:mysql://localhost:3306/sms_management

jdbc.user=root
jdbc.password=toor